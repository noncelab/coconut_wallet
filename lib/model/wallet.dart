import '../utils/colors_util.dart';

class Wallet {
  final String title;
  final String icon;
  final CustomColor color;

  Wallet({
    required this.title,
    required this.icon,
    required this.color,
  });
}
